# Install ruby and bundler and then install with `bundle install`
# Run the local github pages server with `bundle exec jekyll serve`
# To keep rebuilding as you make changes `bundle exec jekyll serve --watch`
source 'https://rubygems.org'

gem 'github-pages'
